'use client';

/**
 * Compatibility entry — page and imports continue to use this path.
 * Implementation lives in ./inner-compass/*
 */
export { InnerCompassExperience as default } from '@/components/inner-compass/InnerCompassExperience';
